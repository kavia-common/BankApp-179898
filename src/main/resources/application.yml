server.port: 8989
server.servlet.context-path: /bank-api

spring.security.user.name: bankapp
spring.security.user.password: changeit

# Enabling H2 Console (Boot 3)
spring:
  h2:
    console:
      enabled: true
      # Path defaults to /h2-console; security config permits this path.
# Swagger UI available at /bank-api/swagger-ui.html or /bank-api/swagger-ui/index.html
